# ------------- Application -------------
NODE_ENV=development
PORT=5000

# ------------- Database ----------------
# For local dev (Docker Compose 'postgres' service)
DATABASE_URL="postgresql://problem5_user:problem5_password@postgres:5432/problem5_db"

# ------------- Auth / JWT --------------
# Change in production!
JWT_SECRET=change-this-in-prod
# Durations can be "15m", "7d", "3600s", etc.
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# ------------- Logging -----------------
# trace | debug | info | warn | error | fatal | silent
LOG_LEVEL=debug
# Pretty logs only in local dev (Pino transport)
PRETTY_LOGS=true

# ------------- CORS --------------------
# Comma-separated list of allowed origins in production.
# In dev/test, if empty, all origins are allowed.
# Example: https://your-frontend.app,https://admin.your-frontend.app
CORS_ORIGINS=http://localhost:5555

# ------------- Rate Limit --------------
# 5 minutes default window, 150 requests per window per IP
RATE_LIMIT_WINDOW_MS=300000
RATE_LIMIT_MAX=150

# ------------- Docker Postgres ---------
POSTGRES_USER=problem5_user
POSTGRES_PASSWORD=problem5_password
POSTGRES_DB=problem5_db
POSTGRES_PORT=5432